# Database Configuration (OPTIONAL)
# By default, the app uses SQLite in-memory database (no setup needed)
# To use Supabase PostgreSQL instead, uncomment and configure below:

# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-anon-key

# Database Connection (OPTIONAL)
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# n8n Main Webhook URL (handles routing to all agents: Coles, Fuel, Maps)
# This is the ONE webhook that receives all requests and routes them internally
# For n8n Cloud (PRODUCTION):
N8N_MAIN_WEBHOOK_URL=https://louisjean.app.n8n.cloud/webhook-test/26b8906b-6df5-4228-9d8b-859118b52338

# For local development with n8n running on localhost:
# N8N_MAIN_WEBHOOK_URL=http://localhost:5678/webhook/chat

# n8n Configuration
N8N_TIMEOUT=30
